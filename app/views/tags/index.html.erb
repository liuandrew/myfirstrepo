<h1>Listing all Tags</h1>
<ul>
<% @tags.each do |tag| %>
    <li><%= link_to tag.name, tag_path(tag) %></li><br />
<% end %>
</ul>
<%= link_to "<<Return to Index", articles_path, class: "new_article" %>